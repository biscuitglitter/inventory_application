extends layout

block content
  h1= title

  ul
    each item in item_list
      li
        a(href=item.url) #{item.title} (#{item.category.name})
        br
        |  Price: #{item.price}$
        br
        |  Number in stock: ##{item.number_in_stock}
        hr

    else
      li There are no items.